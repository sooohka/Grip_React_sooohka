import styled from "styled-components";

const Container = styled.ul`
  display: flex;
  flex-direction: column;
  gap: 1rem;
  padding: 1.6rem;
  padding-top: 8rem;
  height: 100vh;
  overflow-y: scroll;
`;

const S = {
  Container,
};

export default S;
